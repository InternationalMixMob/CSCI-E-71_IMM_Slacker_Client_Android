
machine:
  java:
    version: oraclejdk7


dependencies:
  override:
    - gradle dependencies

test:
  override:
    - gradle test 

  post:
    - cp -R ./build/* $CIRCLE_ARTIFACTS/
 #   - gradle jacocoTestReport coveralls

deployment:
